log:
  level: debug
  disable_sentry: true

db:
  url: postgres://postgres:password@db:5432/usdt_transfers?sslmode=disable

listener:
  addr: :8000

cop:
  disabled: true
  endpoint: "http://..."
  upstream: "http://..."
  service_name: usdt-listener-svc
  service_port: 80
